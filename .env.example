# Environment Variables for IMS Weather Forecast Email Delivery
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git - it's already in .gitignore

# ==============================================================================
# SMTP Configuration
# ==============================================================================

# SMTP server address (for Gmail, use smtp.gmail.com)
SMTP_SERVER=smtp.gmail.com

# SMTP port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# ==============================================================================
# Email Credentials
# ==============================================================================

# Sender email address (the Gmail account sending the forecast)
# Example: mws430170@gmail.com
EMAIL_ADDRESS=your-sender-email@gmail.com

# Gmail App Password (16 characters, no spaces)
# NOT your regular Gmail password - must be an App Password
# Generate at: https://myaccount.google.com/apppasswords
# Requires 2-Step Verification enabled on your Google account
EMAIL_PASSWORD=your-16-char-app-password

# ==============================================================================
# RECIPIENTS CONFIGURATION
# ==============================================================================
#
# Recipients are now configured in recipients.txt (not in .env)
# To set up recipients:
#   1. Copy recipients.txt.example to recipients.txt
#   2. Add email addresses (one per line)
#   3. Lines starting with # are comments
#   4. Empty lines are ignored
#
# Example recipients.txt content:
#   user1@example.com
#   user2@gmail.com
#   # This is a comment
#
# ==============================================================================

# ==============================================================================
# SECURITY NOTES
# ==============================================================================
#
# 1. This file (.env.example) is a TEMPLATE - commit it to git
# 2. Create a copy named .env with your ACTUAL credentials
# 3. The .env file is in .gitignore - it will NEVER be committed
# 4. Verify before commits: git status | grep ".env" should return nothing
# 5. For GitHub Actions: Add these as Repository Secrets (same names)
#
# ==============================================================================
